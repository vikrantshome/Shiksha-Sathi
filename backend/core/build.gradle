plugins {
    id 'java-library'
}

dependencies {
    // Spring Data MongoDB — provides @Document, @Id, @Field, @CreatedDate, @LastModifiedDate etc.
    api 'org.springframework.boot:spring-boot-starter-data-mongodb'
}
