services:
  mongodb:
    image: mongo:7.0.4
    container_name: shikshasathi-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
